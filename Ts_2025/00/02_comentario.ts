// Esta función suma dos números
function sum(a: number, b: number): number {
  return a + b;
}

/*
  Esta función multiplica dos números.
  Puedes usarla para cualquier tipo de cálculo matemático simple.
*/
function multiplicar(a: number, b: number): number {
  return a * b;
}

/**
 * Resta dos números.
 * @param a - El número del que se resta.
 * @param b - El número a restar.
 * @returns El resultado de a - b.
 */
function restar(a: number, b: number): number {
  return a - b;
}

//RECUERDEN 
//Compilar el proyecto  npx  tsc -p .  
//Compilar poryecto tsc -p  .
//Compilar un fichero tsc 02_cometario.ts
